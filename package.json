{
  "name": "homehero",
  "version": "1.0.0",
  "private": true,
  "description": "HomeHero — on-demand home services marketplace. Monorepo: backend/ (API + microservices) and frontend/ (React app).",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:all": "bash scripts/dev-all.sh",
    "dev:backend": "npm --prefix backend run api",
    "dev:frontend": "npm --prefix frontend run dev",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:seed": "npm --prefix backend run db:seed",
    "db:backfill-experts": "npm --prefix backend run db:backfill-experts",
    "build": "npm --prefix frontend run build"
  }
}
